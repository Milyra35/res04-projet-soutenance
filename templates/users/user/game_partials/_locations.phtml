<article id="locations" class="locations">
    <?php
        $player = $data['locations'];
    ?>
    <h3 class="title">Locations discovered</h3>
    <section class="events">
        <ul>
            <?php
                foreach($player as $locations)
                {
                    if($locations->getIsDiscovered() ===  true)
                    {
                        echo "<li>
                                <img src='/res04-projet-soutenance/assets/images/".$locations->getName().".png' alt='Picture in game of ".$locations->getName()."' />
                                <p>".$locations->getName()." has been discovered.</p>
                            </li>";
                    }
                    else
                    {
                        echo "<li>
                                <img src='/res04-projet-soutenance/assets/images/".$locations->getName().".png' alt='Picture in game of ".$locations->getName()."' />
                                <p>".$locations->getName()." has not been discovered yet.</p>
                            </li>";
                    }
                }
            ?>
        </ul>
    </section>
</article>